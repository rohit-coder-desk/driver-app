export const SocketService = {
  connect: (token: string) => {
    console.log('Socket.IO connection request (Future feature) with token:', token);
  },
  disconnect: () => {
    console.log('Socket.IO disconnected (Future feature)');
  },
};
